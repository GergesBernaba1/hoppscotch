# Baseline Migration

This migration serves as a baseline for the current database schema as of 2025-07-07.

It marks the starting point for tracked migrations using Prisma Migrate.
No schema changes are applied by this migration, as it represents the existing database state.

Future migrations should be created using:
`
npx prisma migrate dev --name your_migration_name
`
